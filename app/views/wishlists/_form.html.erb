<%= form_with(model: wishlist, local: true) do |form| %>
  <% if wishlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wishlist.errors.count, "error") %> prohibited this wishlist from being saved:</h2>

      <ul>
      <% wishlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, class:"form-control form-signin" %>
    <%= form.text_field :name, class:"form-control form-signin", value: params[:title] %>
  </div>

  <div class="field">
    <%= form.label :item_type, class:"form-control form-signin" %>
    <%= form.text_field :item_type, class:"form-control form-signin", value: params[:type] %>
  </div>

  <div class="field">
    <%= form.label :other, class:"form-control form-signin" %>
    <%= form.text_field :other, class:"form-control form-signin" %>
  </div>

  <div class="actions">
    <%= form.submit "Create Wishlist Item", class:"btn btn-sm btn-primary btn-block form-signin"%>
  </div>
<% end %>
